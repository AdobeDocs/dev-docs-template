---
name: Staging
on:
  workflow_dispatch:

jobs:
  deployment:
    name: Deployment
    uses: AdobeDocsPrivate/adp-devsite-workflow-private/.github/workflows/deploy-v3.yml@main
    secrets: inherit
    with:
      env: stage
